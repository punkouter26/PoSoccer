# Archived training configs

Superseded by later phases. Kept rather than deleted because they are cheap
documentation of what was already tried - several encode assumptions that
later runs disproved, and re-deriving that costs more than the disk does.

Do not run these expecting current behaviour: the observation contract
changed on 2026-08-27 (108 -> 178 inputs), so every one of them targets a
model shape the runtime no longer produces.
